{{- if not .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata: {name: {{ include "csa.name" . }}-bootstrap, namespace: {{ .Release.Namespace }}}
stringData: {token: {{ .Values.bootstrapToken | quote }}}
{{- end }}
